# they are different types of variables

* int variables: it store integers such as 123 or -123 (whole numbers).

* float32: it stores numbers with decimal points examples 2.0.

* string: it stores text such as "hello" and it must be in double quotes.

* bool: store values with TRUE or FALSE

# varaible declaration with initial value

package main 
import "fmt"
func main(){
    var num1 string = "clementina"
    var num2 = "enewa"
    s := 2

    fmt.println("num1")
    fmt.println("num2")
}